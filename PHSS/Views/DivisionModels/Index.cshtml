@model IEnumerable<PHSS.Models.DivisionModel>

@{
    ViewBag.Title = "Index";
}

    <h2>
        <span>
            <a href="~/Data/Index">
                <img src="http://www.iconarchive.com/download/i86010/graphicloads/100-flat-2/arrow-back.ico" alt="Alternate Text" style="height : 50px; width : 50px;" />
            </a>
        </span>Index
    </h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DivisionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DivisionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DivisionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DivisionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DivisionId })
        </td>
    </tr>
}

</table>
